import { Event } from "../types";
import eventData from "../data/events";

const getEvents = (): Event[] => {
  return eventData;
};

const addEvent = () => {
  return null;
};

const updateEvent = () => {
  return null;
};

const deleteEvent = () => {
  return null;
};

export default {
  getEvents,
  addEvent,
  updateEvent,
  deleteEvent,
};
